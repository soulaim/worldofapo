varying vec2 varTexCoord;

uniform vec4 unit_color;
uniform sampler2D tex;
uniform bool active;

void main()
{
	if(active)
	{
		gl_FragColor = unit_color * texture2D(tex, varTexCoord);
	}
	else
	{
		gl_FragColor = unit_color;
	}
}

