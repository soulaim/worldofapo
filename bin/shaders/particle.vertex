
attribute float particleScale;

varying float scale_var;
// varying vec4 v_position;

void main()
{
	gl_Position = ftransform();
	gl_FrontColor = gl_Color;
	scale_var = particleScale;
	// frag_depth = (gl_ModelViewMatrix * gl_Vertex).z;
}

